{
  "name": "ltxiangzi-splash-reminder",
  "version": "1.2.0",
  "description": "Cloudflare Worker splash notice and dynamic redirection portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
